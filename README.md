## Update a user repos' teams with write permissions 

to execute the script:
```
node updateRepos.js --token|-t <token>  --team|-t <team slang> --owner|-o <github_user>
```
core infra team slang is `core-product-infrastructure`;